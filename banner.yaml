document: modulemd
version: 1
data:
    summary: Banner Application Module
    description:  Classic-style banner program similar to the one found in Solaris or AIX.
        The banner program prints a short string to the console in very large
        letters.
    license:
        module: [ MIT ]
    dependencies:
        buildrequires:
            minimal-runtime: master
        requires:
            minimal-runtime: master
    profiles:
        default:
            rpms:
                - banner
    components:
        rpms:
            banner:
                rationale: Core application
                ref: master
